# sql-server docker image

## Build
To build the image  run ./build.sh

## Run
To run the images run ./run


## Environment variables
Add a .env file with this variables:
- ACCEPT_EULA=Y
- SA_PASSWORD=YOUR_PASSWORD
